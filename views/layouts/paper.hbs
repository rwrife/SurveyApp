<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Survey Test App - {{title}}</title>
    <script src="jquery.min.js" type="text/javascript"></script>
    <link rel="stylesheet" href="paper.min.css">
    <link rel="stylesheet" href="survey.css">
</head>
<body>
    <div class="header row col flex-spaces border border-primary">        
        <div class="sm-6">{{title}}</div>
        <div class="sm-6 text-right">
            {{#unless atHome}}
                <a href="/">Home</a> / 
            {{/unless}}            
            {{#if isAuthed}}
                <a href="/logout">Logout</a>
            {{else}}
                <a href="/login">Login</a>
            {{/if}}
        </div>                 
    </div>      
    <div class="container">
        {{#if errors}}
            <div class="error row col alert alert-danger">
                {{errors}}
            </div>
        {{/if}}
        {{{body}}}
    </div>
    <div class="header row col flex-spaces border border-primary">        
        <div class="sm-12">
            <a href="/questions">Edit Questions</a>
        </div>                
    </div>     
</body>
</html>